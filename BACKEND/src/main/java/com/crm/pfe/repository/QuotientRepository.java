package com.crm.pfe.repository;

import com.crm.pfe.entities.Quotient;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QuotientRepository extends JpaRepository<Quotient,Long> {
}
